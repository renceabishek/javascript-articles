/**
 * YOU ARE SPECIAL IF YOU ARE LANDING TO THIS SOURCE BECAUSE YOU HAVE SHOWN KEEN INTREST TO LOOK FOR CODING
 * NOT JUST READING..!
 * 
 * All the elements variables declared in variables.js, you can modify/reuse while u are playing around with this source.
 * I have added all the methods functionality here, uncommand each and every method and see how it behaves, Happy debugging!
 * 
 */

// 1.appendChild()

// parent.appendChild(child2);

// 2.append()

// parent.append(child2,child3,child4);

// 3.prepend()

// parent.prepend(newBorn1, newBorn2)

// 4. after()

// parent.after(sibbling1, sibbling2);

// 5. before()

// parent.before(newsibbling1, newsibbling2)

// 6. insertAdjacentElement() 

/**
parent.insertAdjacentElement("beforebegin", beforeBegin);
parent.insertAdjacentElement("afterbegin", afterBegin);
parent.insertAdjacentElement("beforeend", beforeEnd);
parent.insertAdjacentElement("afterend", afterEnd);
 */

// 7.insertBefore()

//parent.insertBefore(newBorn1, child1)

// 8.replaceChild()

// parent.replaceChild(newBorn, child1);

// 9.replaceChildren()

// parent.replaceChildren(newBorn1, newBorn2);

// 10.replaceChildren()

//parent.replaceWith(sibbling);

// 11.innerHTML

/**
 * 
 * const parentElement = document.getElementById("parent");
   parentElement.innerHTML = `<div id="childnew"> This is HTML content </div>`
 */

